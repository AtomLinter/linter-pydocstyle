language: generic

notifications:
  email:
    on_success: never
    on_failure: change

os:
  - linux
  - osx

git:
  depth: 10

env:
  global:
    - APM_TEST_PACKAGES=""

  matrix:
    - ATOM_CHANNEL=stable
    - ATOM_CHANNEL=beta

script: 'curl -s https://raw.githubusercontent.com/atom/ci/master/build-package.sh | sh'
